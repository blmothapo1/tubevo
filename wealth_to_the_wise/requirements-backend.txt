# filepath: requirements-backend.txt
# Phase 2 — SaaS backend dependencies
# Install alongside Phase 1: pip install -r requirements.txt -r requirements-backend.txt

# ── Web framework ────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Config / validation ──────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# ── Rate limiting ────────────────────────────
slowapi>=0.1.9

# ── Auth
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0

# ── Database (Item 4 prep)
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0          # SQLite for local dev
asyncpg>=0.30.0            # PostgreSQL for production (async)
psycopg2-binary>=2.9.9     # PostgreSQL for Alembic migrations (sync)
alembic>=1.13.0            # Schema migrations

# ── Cloud storage (Cloudflare R2 via S3 API)
boto3>=1.34.0

# ── Payments (Stripe)
stripe>=8.0.0

# ── Google OAuth / YouTube API
google-api-python-client>=2.100.0
google-auth>=2.25.0

# ── Testing ──────────────────────────────────
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.4.0
